{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Homepage","text":""},{"location":"#install-vertex-cli-from-testpypi","title":"Install Vertex-CLI from TestPyPI","text":"<p>Vertex-CLI is a command-line tool that lets you use LLMs to answer queries and debug faster. For example, you can run:</p> <pre><code>tex \"tell me about the solar system\"\n</code></pre> <p></p> <p>Replace <code>\"tell me about the solar system\"</code> with any query you like, and Vertex-CLI will generate an accurate response.</p>"},{"location":"#installation","title":"Installation","text":"<p>To install <code>Vertex-CLI</code> from TestPyPI, use:</p> <p><pre><code>pip install -i https://test.pypi.org/simple/ Vertex-CLI==0.1.37\ntex-init\n</code></pre> Or install Vertex-CLI in editable mode. See the Contributors Guide for details.</p>"},{"location":"#managing-api-keys","title":"Managing API Keys","text":"<ul> <li>Add a model and its API key: <pre><code>tex --config &lt;model-name&gt; &lt;api-key&gt;\n</code></pre></li> <li>Remove a model: <pre><code>tex --remove &lt;model-name&gt;\n</code></pre></li> <li>Select a model: <pre><code>tex --select\n</code></pre></li> </ul>"},{"location":"#documentation","title":"Documentation","text":"<ul> <li>Full CLI tool docs: CLI Tool Docs</li> <li>Contribution guide: Contributors Guide</li> <li>API reference: API Reference</li> </ul>"},{"location":"cli_tool_docs/","title":"CLI Documentation","text":""},{"location":"cli_tool_docs/#commands","title":"Commands","text":"<ul> <li> <p>Convert an array to a NumPy array <pre><code>tex \"how to convert an array into a NumPy array\"\n</code></pre></p> </li> <li> <p>Manage API keys for models</p> </li> <li>Add a model and its API key:     <pre><code>tex --config &lt;model-name&gt; &lt;api-key&gt;\n</code></pre></li> <li>Remove a model:     <pre><code>tex --remove &lt;model-name&gt;\n</code></pre></li> <li>List all saved models and their API keys:     <pre><code>tex --list\n</code></pre></li> <li>Select a model to use:     <pre><code>tex --select\n</code></pre></li> <li>Show available commands:     <pre><code>tex --help\n</code></pre></li> </ul>"},{"location":"cli_tool_docs/#debugging-beta-feature","title":"Debugging (Beta Feature)","text":"<p>To check for issues in the last two commands, use: <pre><code>tex --debug\n</code></pre></p> <p>To debug a specific number of recent commands, add a number after <code>--debug</code>. For example, to check the last five commands: <pre><code>tex --debug --5\n</code></pre></p> <p>You can also add a custom message to explain your assumptions or observations: <pre><code>tex \"I think this issue might be related to environment variables\" --debug\n</code></pre></p>"},{"location":"contributors_guide/","title":"Installing the Editable Version of Vertex-CLI","text":""},{"location":"contributors_guide/#1-fork-and-clone-the-repository","title":"1. Fork and Clone the Repository","text":"<p>First, fork the repository on GitHub. Then, clone it to your local machine:</p> <pre><code>git clone https://github.com/[YourUserName]/Vertex-CLI\ncd Vertex-CLI\n</code></pre>"},{"location":"contributors_guide/#2-install-in-editable-mode","title":"2. Install in Editable Mode","text":"<p>Run the following command to install the package in editable mode:</p> <pre><code>pip install -e .\n</code></pre> <p>This allows you to make changes to the code and use them immediately without reinstalling.</p>"},{"location":"contributors_guide/#3-initialize-the-cli","title":"3. Initialize the CLI","text":"<p>Run this command to set up the CLI and install necessary dependencies. It will add a free API key for you to get started:</p> <pre><code>tex-init\n</code></pre>"},{"location":"contributors_guide/#4-configure-the-cli-with-your-model-optional","title":"4. Configure the CLI with Your Model (Optional)","text":"<p>If you have your own model API key, you can configure it like this:</p> <pre><code>tex --config gemini-1.5-flash &lt;model-api-key&gt;\n</code></pre> <p>Replace <code>gemini-1.5-flash</code> with your model's name and <code>&lt;model-api-key&gt;</code> with your actual API key.</p>"},{"location":"references/","title":"API Reference","text":""},{"location":"references/#cli.ai_models","title":"<code>ai_models</code>","text":"<p>AI Models Management Module</p> <p>This module handles the configuration and interaction with various AI models, primarily focusing on Google's Generative AI integration. It manages API keys, model configurations, and generation of AI responses.</p>"},{"location":"references/#cli.ai_models.api_key_model_selection","title":"<code>api_key_model_selection(model_name)</code>","text":"<p>Retrieve API key for specified model.</p> <p>Parameters:</p> Name Type Description Default <code>model_name</code> <code>str</code> <p>Name of the AI model.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>API key associated with the model.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If model is not found or has no API key.</p> Example <p>api_key_model_selection('gemini-1.5-flash') 'your_api_key_here'</p> Source code in <code>cli/ai_models.py</code> <pre><code>def api_key_model_selection(model_name):\n    \"\"\"\n    Retrieve API key for specified model.\n\n    Args:\n        model_name (str): Name of the AI model.\n\n    Returns:\n        str: API key associated with the model.\n\n    Raises:\n        ValueError: If model is not found or has no API key.\n\n    Example:\n        &gt;&gt;&gt; api_key_model_selection('gemini-1.5-flash')\n        'your_api_key_here'\n    \"\"\"\n    models_api_dict = load_models_api()\n    if model_name in models_api_dict and models_api_dict[model_name]:\n        return models_api_dict[model_name]\n    else:\n        raise ValueError(\n            f\"Model '{model_name}' is not available or has no API key. Please add it.\"\n        )\n</code></pre>"},{"location":"references/#cli.ai_models.configure_model","title":"<code>configure_model(model_name, api_key)</code>","text":"<p>Add or update a model's API key configuration.</p> <p>Parameters:</p> Name Type Description Default <code>model_name</code> <code>str</code> <p>Name of the model to configure.</p> required <code>api_key</code> <code>str</code> <p>API key for the model.</p> required Example <p>configure_model('gemini-1.5-flash', 'your_api_key_here') \"Model added successfully.\"</p> Source code in <code>cli/ai_models.py</code> <pre><code>def configure_model(model_name, api_key):\n    \"\"\"\n    Add or update a model's API key configuration.\n\n    Args:\n        model_name (str): Name of the model to configure.\n        api_key (str): API key for the model.\n\n    Example:\n        &gt;&gt;&gt; configure_model('gemini-1.5-flash', 'your_api_key_here')\n        \"Model added successfully.\"\n    \"\"\"\n    models_api_dict = load_models_api()\n    models_api_dict[model_name] = api_key\n    update_models_api_json(models_api_dict)\n    print(\"Model added successfully.\")\n</code></pre>"},{"location":"references/#cli.ai_models.create_json_file","title":"<code>create_json_file()</code>","text":"<p>Create a JSON file with the specified default configuration.</p> <p>Parameters:</p> Name Type Description Default <code>FILE_NAME</code> <code>str</code> <p>The name of the file to be created.</p> required <code>default_config</code> <code>dict</code> <p>The default configuration to write to the file.</p> required Source code in <code>cli/ai_models.py</code> <pre><code>def create_json_file():\n    \"\"\"\n    Create a JSON file with the specified default configuration.\n\n    Args:\n        FILE_NAME (str): The name of the file to be created.\n        default_config (dict): The default configuration to write to the file.\n    \"\"\"\n    # A testing api key for gemini-1.5-flash is added.\n    default_config = {\n        \"selected_model\": None,\n        \"gemini-1.5-flash\": \"AIzaSyCSXtRAITXfGuarMHI1j-0QyKkoT9mUfz8\",\n        \"gemini-1.5-interactive\": None,\n        \"gemini-1.5-creative\": None,\n    }\n\n    with open(FILE_NAME, \"w\") as f:\n        json.dump(default_config, f, indent=4)\n    print(\"JSON file created successfully.\")\n</code></pre>"},{"location":"references/#cli.ai_models.generate_output","title":"<code>generate_output(model_name, prompt_by_user)</code>","text":"<p>Generate AI response using specified model.</p> <p>Parameters:</p> Name Type Description Default <code>model_name</code> <code>str</code> <p>Name of the AI model to use.</p> required <code>prompt_by_user</code> <code>str</code> <p>User's input prompt.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Generated response text.</p> Source code in <code>cli/ai_models.py</code> <pre><code>def generate_output(model_name, prompt_by_user):\n    \"\"\"\n    Generate AI response using specified model.\n\n    Args:\n        model_name (str): Name of the AI model to use.\n        prompt_by_user (str): User's input prompt.\n\n    Returns:\n        str: Generated response text.\n    \"\"\"\n    from cli.llm import gemini_api_output\n\n    stop_spinner = threading.Event()\n    spinner_thread = threading.Thread(target=spin_loader, args=(stop_spinner,))\n    spinner_thread.start()\n    #############################\n\n    output = gemini_api_output(model_name, prompt_by_user)\n\n    #############################\n    stop_spinner.set()\n    spinner_thread.join()\n\n    return output\n</code></pre>"},{"location":"references/#cli.ai_models.list_models","title":"<code>list_models()</code>","text":"<p>List all available models and their API keys.</p> Example <p><code>tex --list</code> gemini-1.5-flash: your_api_key_here</p> Source code in <code>cli/ai_models.py</code> <pre><code>def list_models():\n    \"\"\"\n    List all available models and their API keys.\n\n    Example:\n        ``tex --list``\n        gemini-1.5-flash: your_api_key_here\n    \"\"\"\n    models_api_dict = load_models_api()\n    if models_api_dict:\n        for model, api_key in models_api_dict.items():\n            print(f\"{model}: {api_key}\")\n    else:\n        print(\"No models found.\")\n</code></pre>"},{"location":"references/#cli.ai_models.load_models_api","title":"<code>load_models_api()</code>","text":"<p>Load API configurations from JSON file.</p> <p>Returns:</p> Name Type Description <code>dict</code> <p>Dictionary containing model names and their API keys.   Returns None if file not found.</p> Example <p>load_models_api()</p> Source code in <code>cli/ai_models.py</code> <pre><code>def load_models_api():\n    \"\"\"\n    Load API configurations from JSON file.\n\n    Returns:\n        dict: Dictionary containing model names and their API keys.\n              Returns None if file not found.\n\n    Example:\n        &gt;&gt;&gt; load_models_api()\n        {'gemini-1.5-flash': 'api_key_123', 'gpt-4': 'api_key_456'}\n    \"\"\"\n    try:\n        with open(FILE_NAME, \"r\") as f:\n            return json.load(f)\n    except FileNotFoundError:\n        pass\n</code></pre>"},{"location":"references/#cli.ai_models.remove_model","title":"<code>remove_model(model_name)</code>","text":"<p>Remove a model from the configuration.</p> <p>Parameters:</p> Name Type Description Default <code>model_name</code> <code>str</code> <p>Name of the model to remove.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If model is not found in configuration.</p> Example <p>remove_model('gemini-1.5-flash') \"Model 'gemini-1.5-flash' removed successfully.\"</p> Source code in <code>cli/ai_models.py</code> <pre><code>def remove_model(model_name):\n    \"\"\"\n    Remove a model from the configuration.\n\n    Args:\n        model_name (str): Name of the model to remove.\n\n    Raises:\n        ValueError: If model is not found in configuration.\n\n    Example:\n        &gt;&gt;&gt; remove_model('gemini-1.5-flash')\n        \"Model 'gemini-1.5-flash' removed successfully.\"\n    \"\"\"\n    models_api_dict = load_models_api()\n    if model_name in models_api_dict:\n        del models_api_dict[model_name]\n        update_models_api_json(models_api_dict)\n        print(f\"Model '{model_name}' removed successfully.\")\n    else:\n        raise ValueError(f\"Model '{model_name}' is not found.\")\n</code></pre>"},{"location":"references/#cli.ai_models.update_models_api_json","title":"<code>update_models_api_json(updated_json_data)</code>","text":"<p>Update the models API configuration JSON file.</p> <p>Parameters:</p> Name Type Description Default <code>updated_json_data</code> <code>dict</code> <p>Updated configuration data to be written to file.</p> required Source code in <code>cli/ai_models.py</code> <pre><code>def update_models_api_json(updated_json_data):\n    \"\"\"\n    Update the models API configuration JSON file.\n\n    Args:\n        updated_json_data (dict): Updated configuration data to be written to file.\n\n    \"\"\"\n    with open(FILE_NAME, \"w\") as f:\n        json.dump(updated_json_data, f, indent=4)\n</code></pre>"},{"location":"references/#cli.llm","title":"<code>llm</code>","text":""},{"location":"references/#cli.llm.gemini_api_output","title":"<code>gemini_api_output(model_name, prompt_by_user)</code>","text":"<p>Generate AI response using specified model.</p> <p>Parameters:</p> Name Type Description Default <code>model_name</code> <code>str</code> <p>Name of the AI model to use.</p> required <code>prompt_by_user</code> <code>str</code> <p>User's input prompt.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Generated response text.</p> Source code in <code>cli/llm.py</code> <pre><code>def gemini_api_output(model_name, prompt_by_user):\n    \"\"\"\n    Generate AI response using specified model.\n\n    Args:\n        model_name (str): Name of the AI model to use.\n        prompt_by_user (str): User's input prompt.\n\n    Returns:\n        str: Generated response text.\n    \"\"\"\n    api_key = api_key_model_selection(model_name)\n\n    client = genai.Client(api_key=api_key)\n    response = client.models.generate_content(model=model_name, contents=prompt_by_user)\n\n    return response.text\n</code></pre>"},{"location":"references/#cli.prettify_llm_output","title":"<code>prettify_llm_output</code>","text":""},{"location":"references/#cli.prettify_llm_output.prettify_llm_output","title":"<code>prettify_llm_output(response)</code>","text":"<p>Prettifies the output from a language model response by stripping leading and trailing whitespace and code block markers, then prints it as Markdown to the console.</p> <p>Parameters:</p> Name Type Description Default <code>response</code> <code>str</code> <p>The raw response from the language model.</p> required <p>Returns:</p> Type Description <p>None</p> Source code in <code>cli/prettify_llm_output.py</code> <pre><code>def prettify_llm_output(response):\n    \"\"\"\n    Prettifies the output from a language model response by stripping leading\n    and trailing whitespace and code block markers, then prints it as Markdown\n    to the console.\n\n    Args:\n        response (str): The raw response from the language model.\n\n    Returns:\n        None\n    \"\"\"\n    markdown_output = response.strip().strip(\"```\")\n    console = Console()\n    md = Markdown(markdown_output)\n    print()\n    console.print(md)\n    print()\n</code></pre>"},{"location":"references/#cli.prompt","title":"<code>prompt</code>","text":""},{"location":"references/#cli.prompt.handle_all_quries","title":"<code>handle_all_quries()</code>","text":"<p>Handles all user queries from the command line prompt.</p> <p>This function performs the following steps: 1. Retrieves the user command line prompt and input flags. 2. If a prompt is provided by the user, it processes the prompt for the LLM. 3. If no prompt is provided but the 'debug' flag is present, it triggers the debug function for the last command line prompt. 4. Handles any additional input flags provided by the user.</p> <p>Returns:</p> Type Description <p>None</p> Source code in <code>cli/prompt.py</code> <pre><code>def handle_all_quries():\n    \"\"\"\n    Handles all user queries from the command line prompt.\n\n    This function performs the following steps:\n    1. Retrieves the user command line prompt and input flags.\n    2. If a prompt is provided by the user, it processes the prompt for the LLM.\n    3. If no prompt is provided but the 'debug' flag is present, it triggers the debug function for the last command line prompt.\n    4. Handles any additional input flags provided by the user.\n\n    Returns:\n        None\n    \"\"\"\n    prompt_by_user, all_input_flags = user_command_line_prompt()\n    if prompt_by_user:\n        prompt_for_llm(prompt_by_user)\n    elif len(all_input_flags) &gt; 1 and all_input_flags[1] == \"debug\":\n        debug_last_command_line_prompt(prompt_by_user, all_input_flags)\n    handle_input_flags(all_input_flags)\n</code></pre>"},{"location":"references/#cli.prompt.handle_input_flags","title":"<code>handle_input_flags(all_input_flags)</code>","text":"<p>Processes the input flags provided by the user and performs corresponding actions.</p> <p>Parameters:</p> Name Type Description Default <code>all_input_flags</code> <code>list</code> <p>A list of input flags provided by the user.</p> required Source code in <code>cli/prompt.py</code> <pre><code>def handle_input_flags(all_input_flags):\n    \"\"\"\n    Processes the input flags provided by the user and performs corresponding actions.\n\n    Args:\n        all_input_flags (list): A list of input flags provided by the user.\n    \"\"\"\n    if all_input_flags:\n        if not all_input_flags[0] == \"\":\n            print(\n                \"Prompt should be quoted in double quotes, and the flags must be spaced out\"\n            )\n\n        for flag in all_input_flags:\n            flags_list = flag.split(\" \")\n            if flag.startswith(\"config\"):\n                configure_model(flags_list[1], flags_list[2])\n                print(\n                    f\"Configured model: {flags_list[1]} with API key: {flags_list[2]}\"\n                )\n            elif flag == \"list\":\n                print(\"Listing all models:\")\n                ai_models.list_models()\n            elif flag.startswith(\"remove\"):\n                print(\"Removing model:\", flags_list[1])\n                remove_model(flags_list[1])\n            elif flag.startswith(\"select\"):\n                ai_models.select_model(flags_list[1])\n            elif flag == \"help\":\n                print(\"Usage: python3 main.py &lt;prompt&gt;\")\n                print(\"Example: python3 main.py 'How are you?'\")\n                print(\"Flags are: --config &lt;model_name&gt; &lt;api_key&gt;, remove &lt;model_name&gt;\")\n                print()\n</code></pre>"},{"location":"references/#cli.prompt.last_command_line_prompt","title":"<code>last_command_line_prompt(last_number_of_commands)</code>","text":"<p>Retrieves the last N commands from the user's shell history.</p> <p>Parameters:</p> Name Type Description Default <code>last_number_of_commands</code> <code>int</code> <p>The number of recent commands to retrieve.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>A string containing the last N commands.</p> Source code in <code>cli/prompt.py</code> <pre><code>def last_command_line_prompt(last_number_of_commands):\n    \"\"\"\n    Retrieves the last N commands from the user's shell history.\n\n    Args:\n        last_number_of_commands (int): The number of recent commands to retrieve.\n\n    Returns:\n        str: A string containing the last N commands.\n    \"\"\"\n    history_file = os.path.expanduser(\"~/.bash_history\")\n    with open(history_file, \"r\") as file:\n        history_lines = file.readlines()\n    last_commands = history_lines[-last_number_of_commands:]\n    return \"\".join(last_commands)\n</code></pre>"},{"location":"references/#cli.prompt.prompt_for_llm","title":"<code>prompt_for_llm(prompt_for_llm)</code>","text":"<p>Generates and displays the output from the language model based on the user's prompt.</p> <p>Parameters:</p> Name Type Description Default <code>prompt_for_llm</code> <code>str</code> <p>The prompt to send to the language model.</p> required Source code in <code>cli/prompt.py</code> <pre><code>def prompt_for_llm(prompt_for_llm):\n    \"\"\"\n    Generates and displays the output from the language model based on the user's prompt.\n\n    Args:\n        prompt_for_llm (str): The prompt to send to the language model.\n    \"\"\"\n    prompt_for_llm += \" give response in short form, if asked for commands then give commands and dont explain too much\"\n\n    models_api_dict = load_models_api()\n    if models_api_dict[\"selected_model\"] is None:\n        model_name = \"gemini-1.5-flash\"  # default\n    else:\n        model_name = models_api_dict[\"selected_model\"]\n\n    response = generate_output(model_name, prompt_for_llm)\n    prettify_llm_output(response)\n</code></pre>"},{"location":"references/#cli.prompt.user_command_line_prompt","title":"<code>user_command_line_prompt()</code>","text":"<p>Parses command-line arguments provided by the user.</p> <p>Returns:</p> Name Type Description <code>tuple</code> <p>A tuple containing: - prompt_by_user (str or None): The user's prompt, if provided. - all_input_flags (list): A list of input flags provided by the user.</p> Source code in <code>cli/prompt.py</code> <pre><code>def user_command_line_prompt():\n    \"\"\"\n    Parses command-line arguments provided by the user.\n\n    Returns:\n        tuple: A tuple containing:\n            - prompt_by_user (str or None): The user's prompt, if provided.\n            - all_input_flags (list): A list of input flags provided by the user.\n    \"\"\"\n    args = [x for x in sys.argv]\n    load_models_api()\n\n    if len(args) &gt; 1 and not args[1].startswith(\"--\"):\n        prompt_by_user = args[1]\n        entire_cmd_command = \" \".join(args[2:])\n    else:\n        prompt_by_user = None\n        entire_cmd_command = \" \".join(args[1:])\n\n    all_input_flags = entire_cmd_command.split(\"--\")\n    all_input_flags = [x.strip() for x in all_input_flags]\n\n    return prompt_by_user, all_input_flags\n</code></pre>"},{"location":"references/#cli.utils","title":"<code>utils</code>","text":""},{"location":"references/#cli.utils.install_requirements","title":"<code>install_requirements()</code>","text":"<p>Installs the required dependencies for the application.</p> Source code in <code>cli/utils.py</code> <pre><code>def install_requirements():\n    \"\"\"\n    Installs the required dependencies for the application.\n    \"\"\"\n\n    dependencies = [\"rich\", \"google-genai\"]\n    for package in dependencies:\n        subprocess.run([sys.executable, \"-m\", \"pip\", \"install\", package])\n\n    from cli.ai_models import create_json_file\n\n    create_json_file()\n</code></pre>"},{"location":"references/#cli.utils.spin_loader","title":"<code>spin_loader(stop_event)</code>","text":"<p>Displays a spinning loader in the console until the stop_event is set.</p> <p>Parameters:</p> Name Type Description Default <code>stop_event</code> <code>Event</code> <p>An event object used to signal the loader to stop.</p> required Source code in <code>cli/utils.py</code> <pre><code>def spin_loader(stop_event):\n    \"\"\"\n    Displays a spinning loader in the console until the stop_event is set.\n\n    Args:\n        stop_event (threading.Event): An event object used to signal the loader to stop.\n    \"\"\"\n    spinner = itertools.cycle([\"-\", \"/\", \"|\", \"\\\\\"])\n    while not stop_event.is_set():\n        sys.stdout.write(next(spinner))\n        sys.stdout.flush()\n        time.sleep(0.1)\n        sys.stdout.write(\"\\b\")\n    sys.stdout.write(\" \")\n    sys.stdout.flush()\n</code></pre>"}]}